# Claude Code Config Script

Ensure the `npm` has been installed (should be done when installing nvim), if not:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.2/install.sh | bash
\. "$HOME/.nvm/nvm.sh"
nvm install 22
```

